version: "3.9"
services:
  teamvault:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "8080:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=change-me
      - DB_FILE=/data/teamvault.db
    volumes:
      - ./data:/data
    restart: unless-stopped
