{
  "name": "secret-flight",
  "version": "1.0.0",
  "description": "Dead-man switch via IPFS & smart-contract",
  "type": "module",
  "scripts": {
    "dev": "nodemon backend/src/index.js",
    "contract:deploy": "node contract/deploy.js",
    "docker:build": "docker build -t secret-flight backend",
    "docker:run": "docker run --rm -it --env-file backend/.env secret-flight"
  },
  "dependencies": {
    "express": "^4.19.0",
    "node-cron": "^3.0.3",
    "web3": "^4.7.0",
    "ipfs-http-client": "^60.0.1",
    "crypto-js": "^4.2.0",
    "commander": "^11.1.0",
    "nodemailer": "^6.9.13",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
